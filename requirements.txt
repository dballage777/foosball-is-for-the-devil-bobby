# Core
pandas>=2.0
numpy>=1.24
pyarrow>=12.0
requests>=2.28
PyYAML>=6.0

# Modeling
scikit-learn>=1.3
xgboost>=1.7        # optional; pipeline degrades gracefully if unavailable
lightgbm>=4.0       # optional; pipeline degrades gracefully if unavailable

# Analysis / explainability
shap>=0.43          # optional; SHAP report skipped if unavailable
statsmodels>=0.14   # VIF / multicollinearity

# Visualization
matplotlib>=3.7
plotly>=5.15

# Testing
pytest>=7.4
