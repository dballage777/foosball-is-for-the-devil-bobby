# =====================================================================
# Bible Study Platform — environment variables
# Copy to `.env.local` and fill in real values. NEVER commit .env.local.
# All values below are safe placeholders only.
# =====================================================================

# ---- Supabase (Auth + Postgres + Row Level Security) ----
# Public values (safe to expose to the browser):
NEXT_PUBLIC_SUPABASE_URL="https://your-project-ref.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-public-key"

# Server-only. NEVER expose to the client. Used only in trusted server code
# (migrations, admin scripts). Bypasses RLS — handle with extreme care.
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"

# ---- Bible content provider (NIV requires a license — see docs/bible-licensing.md) ----
# Provider abstraction lets you swap implementations. Supported values:
#   "apibible"  -> API.Bible (scripture.api.bible, American Bible Society)
#   "mock"      -> local public-domain sample text for local dev ONLY
BIBLE_PROVIDER="mock"

# API.Bible key (https://scripture.api.bible). Required only when
# BIBLE_PROVIDER="apibible". Does NOT by itself grant NIV rights — the NIV
# is licensed separately by Biblica. See docs/bible-licensing.md.
BIBLE_API_KEY=""
# The Bible edition/version id to request from the provider (e.g. a public
# domain edition for testing, or your licensed NIV edition id).
BIBLE_DEFAULT_VERSION_ID=""

# ---- Audio provider (Listener's Commentary — see docs/audio-licensing.md) ----
# Audio is linked/embedded from the official source, never re-hosted.
# No secret required for official links; document any embed keys here if a
# provider later requires them.

# ---- App ----
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
